CARLA agent code, vendored.

source      CARLA 0.9.15  --  PythonAPI/carla/agents
copied from C:/src_ext/Carla/PythonAPI/carla/agents
files       8, all BYTE-IDENTICAL to the originals (verified by md5sum)
lines       1682
licence     MIT (headers retained in each file)

Nothing under agents/ is modified, and nothing under agents/ should be. Every
line edited here becomes a merge conflict the next time CARLA is upgraded. All
FIXS-specific behaviour lives in fixs_adapter.py, one directory up, as a
subclass -- see its docstring.

WHAT IS HERE, AND WHY EACH FILE
  navigation/basic_agent.py            the agent. run_step's four steps.
  navigation/local_planner.py          the waypoint queue + control dispatch
  navigation/controller.py             VehiclePIDController + the two PIDs
  navigation/global_route_planner.py   NOT used by FIXS -- but basic_agent.py
                                       imports it at module scope and builds one
                                       in __init__ unconditionally, so it has to
                                       be here. Measured cost with the FIXS
                                       adapter's empty topology: 0.1 ms, a
                                       0-node graph, never touched again.
  tools/misc.py                        get_speed, is_within_distance, ...

NOT copied, because nothing here reaches them: behavior_agent.py,
behavior_types.py, constant_velocity_agent.py.

REQUIRES  numpy, networkx (global_route_planner), shapely (basic_agent)

UPGRADING
  Re-copy all 8 files from the new CARLA's PythonAPI/carla/agents, update the
  version above, and re-run the checks in fixs_adapter's __main__ selftest. If a
  copy ever stops being byte-identical, that is a fork and it needs recording
  here with the reason.
